import React from 'react';
import Dashboard from '../Dashboard';

export const SimcarPage: React.FC = () => {
  return <Dashboard initialView="simcar-clip" hideSidebar={true} />;
};

export default SimcarPage;
