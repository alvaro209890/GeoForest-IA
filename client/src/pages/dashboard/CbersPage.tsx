import React from 'react';
import Dashboard from '../Dashboard';

export const CbersPage: React.FC = () => {
  return <Dashboard initialView="cbers-wpm" hideSidebar={true} />;
};

export default CbersPage;
