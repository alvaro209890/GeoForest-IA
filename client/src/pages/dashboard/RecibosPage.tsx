import React from 'react';
import Dashboard from '../Dashboard';

export const RecibosPage: React.FC = () => {
  return <Dashboard initialView="simcar-receipts" hideSidebar={true} />;
};

export default RecibosPage;
